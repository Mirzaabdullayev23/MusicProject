extends layout

block content
    h1.mb-5= title
    form(method="POST" action="/login")
        .form-group
            label(for='username') Username mingizni yozing
            input#username.form-control(type='text' name="username")
        .form-group
            label(for='password') Parolingizni yozing
            input#password.form-control(type='password' name="password")
        button.btn.btn-success(type='submit') Kirish
